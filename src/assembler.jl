
function assembler( nodalCoords, elemNodalConnec, elemMEBIVals, materialsData, elementsData, boundaryCondsData, currentSolution )

    nElems     = size( elemNodalConnec, 1) ;
    nNodes     = size( nodalCoords,     1) ;

    
    
    return Matrix, vector;

end
